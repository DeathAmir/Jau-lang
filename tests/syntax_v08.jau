/* Jau 0.8 syntax smoke test */
fn sum_to(n:int):int {
    var total:int = 0;
    for (var i:int = 0; i < n; i++) {
        if (i == 3) {
            continue;
        }
        total += i;
    }
    total -= 1;
    total++;
    return total;
}

var answer:int = sum_to(6);
print(answer);
